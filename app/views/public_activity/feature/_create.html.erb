added a new

<% if activity.trackable %>
	<%= activity.trackable.feature_type.singularize.downcase %> article called <%= link_to activity.trackable.title, "/" %>
<% else %>
	article which has since been removed from the site.
<% end %><br /><small class="muted"><%= l activity.created_at, format: :feed %></small>