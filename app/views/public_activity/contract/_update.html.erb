updated 

<% if activity.trackable %>
	<%= link_to activity.trackable.player.full_name, activity.trackable.player %>'s <%= link_to "contract", activity.trackable %>.<br /><small class="muted"><%= l activity.created_at, format: :feed %></small>
<% else %>
	a contract which has since been removed from the site.
<% end %>
