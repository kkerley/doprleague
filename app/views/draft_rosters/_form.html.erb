<%= form_for(@draft_roster) do |f| %>
  <% if @draft_roster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@draft_roster.errors.count, "error") %> prohibited this draft_roster from being saved:</h2>

      <ul>
      <% @draft_roster.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :team_id %><br />
    <%= f.number_field :team_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
