<% @activities.each do |activity| %>
  <div class="activity">
    <p class="h5" style="font-weight: normal;"><%= image_tag activity.owner.avatar.url(:mini) %> <%= link_to activity.owner.display_name, activity.owner.team if activity.owner %>
    <%= render_activity activity %>
  </p>
  </div>
<% end %>