<%= form_for(@roster_spot) do |f| %>
  <% if @roster_spot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@roster_spot.errors.count, "error") %> prohibited this roster_spot from being saved:</h2>

      <ul>
      <% @roster_spot.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :draft_roster_id %><br />
    <%= f.number_field :draft_roster_id %>
  </div>
  <div class="field">
    <%= f.label :player_id %><br />
    <%= f.number_field :player_id %>
  </div>
  <div class="field">
    <%= f.label :position %><br />
    <%= f.number_field :position %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
